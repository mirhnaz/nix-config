# PLACEHOLDER — not a real hardware configuration.
#
# Regenerate on the ThinkPad and commit the result (see README, "NixOS setup"):
#   sudo nixos-generate-config --show-hardware-config \
#     > ~/dev/nix-config/nixos/hosts/thinkpad/hardware-configuration.nix
#
# Until then this stub keeps the flake evaluable (`nix flake show`) but the
# mir-nixos-thinkpad system itself cannot be built.
{ ... }:

{ }
